/**
 * Render the "SUNO" brand logo as an SVG text element.
 */
export function renderSunoLogo(x: number, y: number): string {
  return `<text class="suno-logo" x="${x}" y="${y}" text-anchor="end">SUNO</text>`;
}
